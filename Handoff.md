# Handoff.md — antlii-stack faithful-recreation playbook

Living doc for porting the **antlii-stack tools** (the non-"classic" suite) into faithful homages of the originals at antlii.work. One tool per session; update the **Progress tracker** + **Session log** at the bottom when you finish one, so the next session starts fresh with current state.

> Read `CLAUDE.md` (architecture) and `AGENTS.md` first. The legacy raster-tool cleanup that previously lived in this file is in git history.

---

## The core insight (why this works now)

The earlier v1 tools were built from a *written spec* (`antlii-tool-specs.md`) and diverge **structurally** from the originals — different algorithms, not just different tuning. But **antlii ships the full, unminified, modular source of every tool on GitHub Pages.** antlii.work is just a Cargo portfolio that iframes those apps. So we can read the *actual* algorithm and validate our build visually against the live tool. See memory `antlii-source-readable`.

- Tool apps: `https://antlii.github.io/<slug>/` (e.g. `flake-tool`, `boids-tool`, `sampl-tool`, `textr-tool`).
- Per-tool scripts: `https://antlii.github.io/<slug>/scripts/*.js`.
- Shared assets: `https://antlii.github.io/assets/...` (libs, `json/1000-color-palettes.json`).

**Real stack:** p5.js 1.11.2 · Tweakpane 3.1.10 (+essentials/infodump/textarea) · Paper.js 0.12.18 (custom-SVG import + SVG export only) · simplex-noise (4D) seeded by alea · h264-mp4-encoder + p5.capture for MP4.

---

## IP posture (decided with the user — hold this line)

**Study & reimplement** — faithful *behaviour*, original *assets*.
- ✅ Match: algorithms, parameter taxonomy, defaults, ranges, easing, interaction model, render cadence. These are functional.
- ✅ Write our own implementation on our shell.
- ❌ Do NOT copy verbatim: preset **names**, the curated `1000-color-palettes.json`, bespoke shape **art**, branding, the watermark/license/embed-protect scripts. Author our own preset names, palettes, and shape paths.

---

## The shared foundation (`js/antlii/`) — reuse, don't reinvent

| Module | API | Use for |
|---|---|---|
| `shell.js` | `createTool({name,version,backHref})` → `{root, pane, pages:{main,export,options}, canvasHost, startSketch(factory), mountCanvas(), getCanvas(), toggleFullscreen()}` | Every tool. `startSketch` = p5 instance mode; `mountCanvas` = bare canvas for Paper. |
| `export.js` | `attachExport(page, {getCanvas, getSVG?, name})` | PNG (multi-res) + SVG (if `getSVG`) + video (WebM/MP4) + frame-zip. Free for every tool. |
| `presets.js` | `attachPresets(page, {pane, params, presets, randomize?, onApply?})` | Simple flat presets (shallow `Object.assign`). For **nested** state use FLAKE's clean-slate deep-merge pattern (copy it from `flake.js`). |
| `noise.js` | `seedNoise(seed)`, `noise2D/3D/4D(...)`, `alea(seed)→rng` | Seedable simplex. **4D** is for looped motion (2 dims animate). Vendored `simplex-noise` under `js/vendor/simplex/`. |
| `palette.js` | `PALETTES`, `randomPalette(rng?)`, `buildLayers(colors,layers,rng)`, `toTransitionStops(colors)`, `paletteLerp(stops,t)`, `interpolateHex`, `pickColor`, `attachPaletteControls(folder,{palette,pane,onChange})` | Color modes (solid/sequence/transition), curated palettes, swatch + Random/Shuffle UI. |
| `shapes.js` | `SHAPE_OPTS`, `makeShape(type,size)` (Paper item) | Vector tools. |
| `typography.js` | `FONT_OPTIONS`, `loadFont(name)`, `parseFont(buf)`, `textUnits(font,text,size,mode)` | Type tools. |

**Panel:** Tweakpane **4** + an antlii-style dark/compact theme in `css/style.css` (`.antlii-pane { --tp-* }`). We stayed on TP4 (no downgrade); use `addBinding`. For multi-button rows / swatches, use `attachPaletteControls` or plain `addButton`s (the TP3 buttongrid/infodump/textarea plugins are not available).

---

## The method (step by step)

### 0. Setup
- Serve: `python3 -m http.server 8000` (from repo root). Hard-refresh after editing shared modules (no cache headers).
- Playwright MCP is the validation tool. The live antlii tools and `localhost` are both reachable.

### 1. Find & download the real source
Identify the live slug (usually `<name>-tool` or `<name>`), then **confirm it exists** — navigate Playwright to `https://antlii.github.io/<slug>/`. If antlii has no such tool, it's an original Ksawery tool: build by inspiration, note it, skip the source steps.

Get the exact script list (it varies per tool) from the page's network requests, or curl the index and grep `<script src>`. Then download to `/tmp` (reference only — never commit antlii source):
```bash
curl -sS --create-dirs -o "/tmp/antlii-ref/<slug>/scripts/#1.js" \
  "https://antlii.github.io/<slug>/scripts/{var,ui,presets,preset,ease,palette,random,path,svg,image,system,export,events,main}.js"
curl -sS --create-dirs -o "/tmp/antlii-ref/<slug>/index.html" "https://antlii.github.io/<slug>/"
```

### 2. Study — build an accurate mental model
Typical module roles (FLAKE's set; others differ):
- `var.js` — params object with **defaults**, enums/option maps, shape data, preset-name list.
- `ui.js` — every Tweakpane control: exact **ranges/steps**, folder structure.
- `main.js` — `setup`/`draw`, the core algorithm (the heart).
- `presets.js` — named-preset values (study structure; author your own).
- `system.js` — canvas/`gForm` setup, resolutions, pixel density.
- `ease.js`, `palette.js`, `random.js` (randomizer), `path.js`, `svg.js`, `image.js`, `export.js`, `events.js`.

Write down: the algorithm, the full param model + defaults + ranges, the panel layout (tabs/folders), the export options, and **one concrete preset** to use as an A/B target (read its exact values from `presets.js`).

### 3. Reimplement on the shell
- Copy the closest tool-type template: raster → `ritm`, vector → `splitx`, shader → `refract-tool`, type → `textr`, image-manip → `skaaan`, simulation → (see FLAKE/`boids-tool`).
- `tools/<name>/index.html`: TP4 import map + the global libs it needs (p5 / paper-full / opentype) + `<script type="module" src="<name>.js">`.
- `tools/<name>/<name>.js`: `createTool(...)`, build folders/bindings on `pages.main` with the **real ranges/defaults**, `attachExport` (+ `getSVG` for vector), reuse `noise`/`palette`/`shapes`/`typography`. Render via `startSketch` (p5) or `mountCanvas` (Paper).
- Use the real **numeric defaults** (functional). Use **original** preset names/palettes/shape art.
- Leave a dev hook (`window.__<name> = { applyPreset, ...state, renderSVG }`) for A/B testing.

### 4. Validate against the live tool (the key step)
- Serve locally; open `http://localhost:8000/tools/<name>/`. Check console = 0 errors.
- Open the live tool; its top-level functions are global — drive it with `window.loadPreset('<presetKey>')` (preset keys are in the live `var.js` `presetTypes`). Screenshot.
- Set your build to the **same preset values** (via the dev hook) and screenshot. Compare **structure/character** (topology, density, symmetry, color behaviour). The fine noise texture will differ — that's fine.
- Iterate until it's clearly the same family. Save A/B images to `/tmp/antlii-ref/compare/`.

### 5. Finish fully
- Presets (original names) applying onto a **clean default slate**; a striking default; SVG export (vector); drag-drop (custom SVG / image mask) where the original has it; verify across several presets with 0 console errors.
- Add the tool's card to `index.html` + a preview to `js/antlii/previews.js`.
- Move dev screenshots out of the repo (`/tmp/antlii-ref/compare/`); `git status` should show only intended files. **Never `rm -rf` a tracked dir** (the repo tracks `.playwright-mcp/`).

### 6. Update state
- Update the **Progress tracker** + **Session log** below.
- Update memory: `antlii-build-progress` (+ create/adjust others as needed).

---

## Worked example — FLAKE (the template, ✅ complete)

**What it is:** a dense shuffled point field (~5–16k stamps) where ONE alea-seeded **4D-simplex** sample per point drives BOTH the stamp's *size* (a triangle wave within each palette band → concentric rings) AND its *color*, with radial **branch symmetry**, swirl, a parametric polar mask, a raster image mask, and looped motion. Shapes blend on a transparent buffer, then composite over the bg. Paper.js only normalizes dropped custom SVGs.

**Validated:** A/B against the live "Quantum Processing" preset (`window.loadPreset('staticQuantumProcessing')`) — same nested-square rings, quad-dot XOR texture, 3×3 tiling. Images in `/tmp/antlii-ref/compare/`.

**Files:** `tools/flake-tool/{index.html,flake.js}`. It dogfoods the shared `noise` (4D) + `palette` (swatches/Random) + TP4 theme. Note the render-space pattern: compute in a fixed ratio resolution (`gForm`), composite, and CSS-fit the canvas to the viewport so parameter magnitudes match the reference.

---

## Conventions & gotchas

- **No bundler/build**, ever. CDN + import maps; vendored libs (`js/vendor/`) for offline-safe pieces.
- One shared stylesheet (`css/style.css`); no per-tool CSS.
- **Comment-light** code; each `js/antlii/` module gets a short header.
- Verify in a browser via Playwright — there are no tests.
- Cache: `http.server` sends no cache headers → hard-refresh / fresh tab after editing shared modules; confirm a change took by reading state through the dev hook.
- Module-scoped consts aren't on `window` — expose a dev hook for testing, remove before any "ship".
- Tweakpane bindings hold the **object reference** — to swap preset state, mutate objects **in place** (don't reassign `params.x`/`palette.array`), or bindings detach. FLAKE's `resetToDefaults`/`applyPreset` show the in-place deep-merge pattern.

---

## Progress tracker

Foundation: ✅ noise (2D/3D/**4D** + alea, vendored simplex) · ✅ palette system + swatch UI · ✅ TP4 antlii theme. Optional/deferred: h264-mp4-encoder export (ffmpeg.wasm works), generalized clean-slate `presets.js`.

Inventory verified 2026-05-24 (HTTP check of `antlii.github.io/<slug>/`). **All 12 with live antlii sources are now ported** (FLAKE + SPLITX + BOIDS + BLUUR + TEXTR + SAMPL + RASTR + DITHR + BIOM + DRIFT + KLON + SKAAAN done). The other 4 return 404 under every plausible slug — no live original, so they remain original Ksawery tools (build by inspiration; no A/B possible).

| Tool | dir | type | live source | status |
|---|---|---|---|---|
| FLAKE | `flake-tool` | vector/raster | `flake-tool` ✓ | ✅ complete + validated |
| SPLITX | `splitx` | raster+SVG | `splitx-tool` ✓ | ✅ complete + validated |
| BOIDS | `boids-tool` | simulation | `boids-tool` ✓ | ✅ complete + validated |
| BLUUR | `bluur` | shader | `bluur-tool` ✓ | ✅ complete + validated |
| TEXTR | `textr` | type | `textr-tool` ✓ | ✅ complete + validated |
| SAMPL | `sampl` | type | `sampl-tool` ✓ | ✅ complete + validated |
| RASTR | `rastr` | type | `rastr-tool` ✓ | ✅ complete + validated |
| DITHR | `dithr` | shader | `dithr-tool` ✓ | ✅ complete + validated |
| BIOM | `biom` | generative | `biom-tool` ✓ | ✅ complete + validated |
| DRIFT | `drift` | image-manip | `drift-tool` ✓ | ✅ complete + validated |
| KLON | `klon` | image-manip | `klon-tool` ✓ | ✅ complete + validated |
| SKAAAN | `skaaan` | image-manip | `skaaan-tool` ✓ | ✅ complete + validated |
| RITM | `ritm` | raster | — none | ⚪ original Ksawery tool |
| REFRACT | `refract-tool` | shader | — none | ⚪ original Ksawery tool |
| PLAIN | `plain` | p5 3D | — none | ⚪ original Ksawery tool |
| STIIL | `stiil` | image-manip | — none | ⚪ original Ksawery tool |

---

## Session log

- **2026-05-24** — Discovered antlii source is open; established the method. Re-ported **FLAKE** end-to-end (engine, masks, SVG export, custom-SVG drop, 8 presets) and validated A/B. Re-aligned the foundation: `noise.js` 4D+alea (vendored simplex), `palette.js` system + swatch UI, TP4 antlii theme in `css/style.css`. FLAKE dogfoods all three; RITM/SPLITX confirmed unbroken. **Next:** pick a tool from the tracker and run the method.
- **2026-05-25** — Re-ported **SPLITX** end-to-end. The v1 was structurally wrong (radial golden-angle scatter on Paper.js); the real engine stamps ONE base shape `form.count.base`× into an offscreen buffer — each stamp transformed by a *sequenced* base scale (`form.sequence`) + per-channel noise/sin **scale / xmove / ymove / rotate** (each with amp·freq·cycle·phase·speed·seed, ordered forward/backward/equal) + a linear **transition** spread — then the buffer is **split-mirrored** (none/H/V/quad) by clipping each cell and flipping ±1. Colour modes: XOR-cutout (`globalCompositeOperation='xor'`, single palette colour), Sequence, Transition (RGB / LAB). Live render = p5 `createGraphics` buffers + `drawingContext.fill(Path2D,'evenodd')`; **SVG export = Paper.js reconstruction** of the same `formData` (CompoundPath + clip layers), the only faithful way to bake per-stamp transforms + the XOR compound path. Notes: gForm/gDraw run `angleMode(DEGREES)`; `ymove` amplitude multiplies by `formData.width` (not height) — replicated as-is. Authored own shape library (rect/circle/ring/oval/triangle/rhombus/cross/star/hexagon/petals/checker/blob + custom-SVG drop), 6 original-named presets (2 reuse real numeric configs w/ own palettes), original LAB transition (vs their Color.js LCH). Validated A/B vs live `splitVibration` (clean match — orange/black XOR quad-X moiré) and `lotusMetamorphosis` (same family). All 6 presets + every colour mode + all splits + animation + transparent-checker bg + SVG export = 0 console errors. Updated landing preview (`previews.js`) to the nested-contour quad-X. **Next:** pick a tool from the tracker (BOIDS / BLUUR / TEXTR / …) and run the method.
- **2026-05-25** — Re-ported **BOIDS** end-to-end. Real engine = a Reynolds flock (alignment / cohesion / separation, each `setMag(speedMax).sub(vel).max(steering)`, weighted by an **alignment bias** `bias ** (otherVel·thisVel)`) over a 3×3 **spatial-hash grid**, with neighbour **subsampling by `accuracy`** (`step = ceil(candidateCount / 2**accuracy)`), edge **wrap or repel**, mouse attract/repel, per-boid **noise wander**, velocity **drag**, and `lerpAngle` heading easing. Shapes (ellipse/rect/triangle/mixed) are oriented to velocity, **scale-randomised**, **skewed** by speed or angle, and rendered either as vector fill+stroke or as **clipped windows into a dropped image**; colour modes none/single/interp-by-(random/speed/angle). The whole `params → g` derivation lives in `syncGlobals` (vision = `vision·min(w,h)/100`, scale = `10·((w+h)/1280)·scale`, etc.); arrays (pos/vel/type/scale/colour) are p5-`randomSeed`-seeded, sized to `boids.max`, and boids index into them. The V2D class + flock math are adapted from Daniel Huang's MIT boids (the basis antlii credits) — kept attribution. **Port specifics:** runs on the shared p5 shell (instance mode → all p5 calls via a module-level `P`); simulation renders directly on the render-space canvas (CSS-fit), no separate gForm. **Two gotchas:** (1) `params.speed.value` is an interval `{min,max}` that the reference binds with the TP3 Essentials *interval* slider — we lack it, so split into two Speed Min/Max sliders. (2) p5 runs `setup()` **synchronously inside `new p5()`** on some loads but not others → the module-end `applyPreset()` raced `P`/`presets` init (TDZ or null-`P`); fixed by deferring the initial preset to the **first `draw()` tick** via a `pendingPreset` flag (a reusable pattern for shell tools that need post-init bootstrap). We ship **no texture** (antlii's is their asset) → default to vector render + colour bg; drop an image for the texture modes (render=image clips the shape and `copy()`s a window of the image; bg=image draws it; transparent bg = CSS checker on a cleared canvas so PNG export stays transparent). Authored 7 original-named presets (the two image-render originals — jellyGeometry/severance — retuned to vector); **Jelly Bloom** needed a scale/count drop (the original relied on a translucent texture). Validated A/B vs live `defaultPreset` (same 2500-triangle repel-flock behaviour) + `coralStream`/`jellyGeometry` family; speed-colour+speed-skew (fluid streaks) and angle-colour+angle-skew (flow-field) both confirmed. All 7 presets + every shape/colour/edge/ratio + animation = 0 console errors. BOIDS needs only p5 (no Paper/SVG; reference disabled SVG too). Landing card + flocking preview already fit — left as-is. **Next:** pick a tool from the tracker (BLUUR shader / TEXTR type / DITHR raster / …) and run the method.
- **2026-05-25** — Re-ported **BLUUR** end-to-end (v1 was a metaball-field GLSL shader — structurally wrong). **Key insight:** despite the "shader" label, BLUUR's *engine* is a 2D-canvas tool — the reference draws a shuffled grid of forms onto a 2D `gForm` buffer using a **per-shape `ctx.filter = blur(Npx)`** (the signature soft-overlap look) + a per-form `globalCompositeOperation` (blend mode) + Inigo-Quilez **cosine-palette** colours; GLSL is used *only* for the post pass (full-screen gaussian blur + brightness/contrast + film grain). So I ran the whole pipeline on 2D canvas: per-shape blur + blend (heart), then post fx as behaviorally-equivalent native filters (`blur()`+`contrast()`+`brightness()`) + a procedural per-pixel **grain** overlay mixed at its opacity. Ported faithfully: the grid→shuffle→`createForms` per-form noise model (corners/size/angle/offset/blur/blend each seeded by `cnv.seed.base` simplex with per-channel coord offsets), `formCoords/Size/Blur/Color`, `map2` easing, all 12 blend modes → canvas composite ops, the cosine palette (`generatePalette`) + a Lab custom-palette interp (vs their Color.js LCH), palette-derived **pale bg** (own lighten-toward-white method vs their oklch), the full randomizer suite, and custom-SVG drag-drop (Paper-parsed → shape entries; reference's asc/desc/random sort). State objects are shaped **exactly like the reference** (`cnv/form/post/palette/svg/rec`) so reference-style preset objects deep-merge onto a clean-slate default; the dev hook `window.__bluur.applyPreset()` accepts a live antlii preset verbatim for A/B. **Port specifics:** responsive ratio-locked canvas (gForm == display size) so absolute per-form blur-px tracks the reference's own window-size-dependent blur ratio; deferred the opening preset to the first `draw()` tick via `pendingPreset` (the BOIDS init-race pattern). Dropped the `defined`/150-palette mode (antlii's curated JSON — IP) → Procedural + Custom only. Authored 6 original-named presets (Prism Stacks reuses the real `prismaticForms` numeric config w/ own palette handling; others original). Validated A/B vs live `loadPreset('prismaticForms')` — same family (vertical 1×6 stack of heavy-blur soft forms, EXCLUSION colour-mixing, pale-yellow palette ground; exact hues differ since our simplex permutation differs — expected). Citrus Haze (custom MULTIPLY) + Solar Tiles (8×8 BLEND tile grid) + all 6 presets render with **0 console errors**. Updated landing card copy + preview (warm overlapping blurred forms, multiply, pale ground). **Gotcha learned:** OVERLAY/soft blends over a transparent buffer + pale generative colours wash out — authored presets need a covering blend (BLEND/MULTIPLY) or saturated colours. **Next:** pick a tool from the tracker (TEXTR type / SAMPL type / RASTR type / DITHR raster / BIOM / DRIFT / KLON / SKAAAN) and run the method.
- **2026-05-25** — Re-ported **TEXTR** end-to-end (v1 was a ring/grid letter scatter — structurally wrong). Most intricate engine so far: a phrase becomes a **stack of rows** (vertical) or **row of columns** (horizontal) of repeated words/letters; each row's **count** follows an order pattern (none/forward/backward/**backforth**/random → the text mass forms diamonds/hourglasses), and every copy is displaced **perpendicular** to the layout axis by a **sin / doublesin / 4D-noise** wave whose phase comes from the copy index AND the row's position (`yFreq = map(rowPos,0,cnvsize,-PI,PI)*freqY`) → a travelling wave; plus per-copy **scale** modulation, an **amp-easing** envelope (uniform/center/edge/side), **collision** spacing, infinite **scroll**. Ported the full `getTextBounds → generateForms → Form{scroll,update,motion,freqFunction,ampEase,collision,display,getOrderRange}` pipeline + all 24 easings (referenced by name). **Reference uses fontkit** (`font.layout()`); reimplemented glyph metrics on **opentype.js** (our stack): `font.getPath(str,0,0,size)` (baseline y=0, glyph extends up = −y — the same convention the reference flips fontkit into), `.toPathData()` for Path2D + Paper, `getBoundingBox()` for centre/height, `getAdvanceWidth()` for advance. Fixed render-space canvas (RATIOS) CSS-fit to viewport; p5 2D, draw via `ctx.fill(Path2D)` save/translate/scale per copy; **SVG export = Paper.js reconstruction** of the same arrangement (re-runs scroll/update/motion/collision into a clipped layer). State shaped like the reference (`cnv/params`) → presets deep-merge onto a clean-slate default; `window.__textr.applyPreset()` accepts a live antlii preset for A/B. Bootstrap deferred to the first `draw()` tick once the font resolves (`pendingPreset`). **Gotcha (cost a debug cycle):** TP4 `addBinding` on an object value `{min,max}` (e.g. `count.scope`) throws a cryptic minified `n` — TP4 only binds `{x,y,z,w}`; split into two sliders (same lesson as BOIDS' interval). Also the v1 `index.html` loaded paper+opentype but **not p5** — the rewrite uses `startSketch`, so added the p5 global. Ship our own fonts (Space Mono/Anton/Archivo Black/Bungee/Major Mono from `typography.js`) — not the reference's Google-Fonts-API picker (their asset). 6 original-named presets across all 4 text modes + both layouts. A/B-validated by driving the live tool's globals to the same controlled config (vertical/repeatLetter/backforth) with its default Roboto — **same family**: letter-cycling diamond field with sine-wave flow + stray edge singles; density/positions differ from Anton-vs-Roboto metrics + simplex (expected). Lateral Drift (columns spelling the phrase, vertical wave + center-amp envelope) + Diamond Wave (repeated words + scale modulation) render the right character; SVG export = 280 valid paths; 0 console errors. Landing card + preview updated to the kinetic-rows look. **Next:** pick a tool from the tracker (SAMPL type / RASTR type / DITHR raster / BIOM / DRIFT / KLON / SKAAAN) and run the method.
- **2026-05-25** — Re-ported **SAMPL** end-to-end (v1 was a generic dot scatter). Engine = lay a phrase out with **opentype.js** (full `generateFontData`/`generateGlyphs`/`wrapText`: kerning, ligatures, letter/word-space, leading, auto line-break, X/Y alignment, margins, offset) → for each glyph **walk its vector outline with Paper.js** and re-sample into an evenly arc-spaced point field (`distributePoints`: per-subpath, density = `easeInQuad(factor)*2`; the reference does this in a **Web Worker**, here run **synchronously** on the global Paper scope — no worker) → at every point **stamp a shape** (ellipse/rect/triangle/polygon, each with an optional concentric **contour hole** → rings) whose **size / rotation / position** are each modulated by a sin or 3D-noise wave (phase from the point's index along the path `freqIndex` AND the glyph index `glyphIndex`), **shrunk toward path ends** (`easeOutExpo` edge), and filled by one of: none / single / **palette sequence** (`round((i/mult)*rawValue)`) / **palette transition** (`paletteLerp` along the path) / **linear & radial gradient** (per-shape `createLinear/RadialGradient`) / **image texture** (dropped image, `clip`+`drawImage`). The glyph outline itself can be drawn under the field. Ported `frame.js` faithfully into a `frameObject` (`F`) rebuilt on change: `shape.size=map(size.value,0,100,0,font*0.5)`, mod x/y = 1±shape.mod, pos.value=`(value/100)*font*0.66`, the sin→`speed*3`/noise→`value*0.25` quirks, sequence/transition palette-array expansion by `mult`, gradient width/height/sin/cos/offset. **SVG export = Paper.js reconstruction** (port of `svg.js`): clip layers for glyph fill + sample field, one cloned `CompoundPath` per point with the same scale(center-pivot)/rotate/translate + Paper gradient fills. State shaped exactly like the reference (`cnv/params/sample/palette`) so reference preset objects deep-merge onto a clean default; dev hook `window.__sampl.applyPreset()` takes a live antlii preset verbatim for A/B. **Two gotchas, both cost a cycle:** (1) **double pixelDensity scale** — p5 already applies a `pixelDensity` (2×) transform to the canvas's 2D context, so my extra `ctx.scale(pd,pd)` rendered everything at **4×**, shoving content off the bottom-right (looked like an overflow/centering bug). Fix: drive the transform explicitly per frame with `ctx.setTransform(pd,0,0,pd,0,0)` instead of compounding. **TEXTR uses the same `ctx.scale(pd,pd)` pattern — worth re-checking it isn't subtly 4×'d too.** (2) the clean-slate `deepMerge` **reassigned arrays** (`dst[k]=src[k].slice()`), detaching the Tweakpane palette-swatch bindings — the documented in-place-mutation gotcha; fixed by mutating arrays in place (`length=0; push(...)`). Keep all preset palettes **length-5** so the 5 fixed swatch bindings never read `undefined`. Shipped **no sample image** (antlii's is their asset) → image-texture fill works only on a **dropped image** (font/image drag-drop both supported); authored 6 original-named presets covering every fill mode + shape + modulation (Sequence Bloom default, Transition Vision, Wire Glyph stroke-only, Radial Pop gradient, Starfield Type polygon+all-noise, Drift Lines). My display fonts (Anton/Archivo Black) are **far wider** than the reference's narrow Space Grotesk default, so faithful presets need **smaller font sizes** (~100–168, not the reference's 380) or single words overflow (auto-layout only wraps on spaces). A/B-validated vs live `loadPreset('blendedVision')` — same family: thin wide ellipse-**ring** streaks (contour 0.85, mod 0.65) tracing the glyph outlines, transition-coloured along each subpath, edge-shrunk (hues/letterforms differ: Anton vs Space Grotesk + different simplex — expected). All 6 presets render + SVG-export (580–1515 paths) with **0 console errors**. Landing card kept; preview rewritten to a travelling dotted glyph outline (`strokeText` + `setLineDash([0.1,8])` round caps + animated `lineDashOffset`, gradient stroke). **Next:** pick a tool from the tracker (RASTR type / DITHR raster / BIOM / DRIFT / KLON / SKAAAN) and run the method.
- **2026-05-26** — Re-ported **RASTR** end-to-end (v1 was a Paper.js coverage-threshold text→shape grid — structurally wrong). **Engine = text as STENCIL:** a phrase is drawn onto an offscreen p5 buffer (`gText`); a grid of `quantity.x × quantity.y` cells is laid over it and **a particle is born wherever the text's alpha ≠ 0** (the sampled colour is *not* used — text only decides *where* shapes appear). Each particle stamps ONE shape (rect / triangle / ellipse / **text-string** glyph), sized from `form.size.base = (w+h)/2 / ((qx·rw+qy·rh)/2)` (so density follows grid count), coloured by **none / single / shuffle (one→two mixed across N then p5-`shuffle`d, seeded) / gradient (per-shape `createLinearGradient` via `drawingContext`)**, and displaced by THREE composable motion systems: **RANDOM** (per-particle `noise3D` flow with a per-particle random phase `flow.pi` + an `easingFlow` envelope along particle index), **NOISE** (looped `noise4D` field, 2 dims animate), **SINE** (sin/cos/wobble periodic; frequency mapped from particle *index* `(i+n/2)/n`). Plus a **`dimension` spread** `(this.x-offset)·dim` that pushes particles out from centre. The `cnv.offset = 2048` trick: particles store `base+2048` so noise samples at large positive coords, then `translate(-offset)` recenters. (The reference's PHYSICS/`interact` motion tab is **commented out in the live UI** → omitted to match.) **SVG export = manual string building** (port of `svg.js`; the reference loads **no Paper.js**) — one `<rect>/<ellipse>/<polygon>/<path>` per particle + per-shape `<linearGradient>`, mirroring the canvas transform. **Port stack = p5 only.** Big realisation: the reference renders text with `textFont(familyString)` — a **FontFace family name, not a p5.Font** — i.e. *native* canvas `fillText`, which is why letter-spacing / box-wrap / align work. So I **register each font as a `FontFace` (native raster render) and `opentype.parse` the same buffer** (glyph path for SVG + char bounds) from one fetch; **dropped p5.loadFont and Paper.js entirely.** Collapsed the reference's `gForm` into the display canvas (render-space = display, CSS-fit; `gText` stays offscreen for sampling); sample via one `loadPixels()` + flat-array alpha reads (fast) not per-cell `get()`. Drew through **p5's API + `drawingContext` gradients (never raw `getContext`)** → sidesteps SAMPL's double-pixelDensity 4× gotcha by construction. **Gotcha (parse-time SyntaxError, nothing ran):** declared `GH` twice — font-URL base const *and* graphics-height var — renamed the font base to `GF`. **A/B method note for Google-Fonts-picker tools:** the live RASTR depends on the Google Webfonts API via **XHR, which hung in headless** (while `fetch` to the same URL returned 200); drove the live tool by manually `fetch`-ing the API, assigning `window.fontList`, then `updateFontFamily()`/`loadPreset()`. Validated A/B vs live `loadPreset('isometricPreset')` — **geometry near-identical: 716 (live) vs 724 (mine) particles, vertical span 349.1 == 349.1 px, 73% coverage** (the apparent size gap in screenshots was only the two tools' different canvas display-fit). Also vs `oneRasterADayPreset` — same family (single black fill + cyan→magenta **gradient-stroke ringed ellipses**, random arrange; my **Anton** stands in for the live **Tajawal** since my set lacks it → letter legibility differs, expected). Shipped my own 6 OFL fonts (Anton / Archivo Black / **Aldrich** / Space Mono / Bungee / Major Mono — FontFace+opentype, *not* the live tool's Google-Fonts-API picker + key, their asset); Aldrich happens to match the isometric preset's face → near-exact A/B. Authored 6 original-named presets — **Isometric Drift** (default; Aldrich rotated-rect gradient+stroke extrusion, backward-arranged for depth), Vector Lullaby, Heat Signature (shuffle embers + flow/noise melt), Static Bloom (gradient + spread + noise), Glitch Banner (sine), Tide Lines (triangle + shuffle) — covering all 4 shapes, all 4 fill/stroke modes, flow/noise/sine motion, every ratio. **'transition' is a SAMPL mode, not RASTR** → `applyPreset` maps a live-preset `transition` → `shuffle`. Two **preset-tuning** fixes after visual check (not engine bugs): Glitch Banner's wide rects (`size.x 1.8`) on a dense grid **smeared into solid scanlines** — switched to ellipse dots + sine (**lesson: shape size must stay ≲ grid spacing or stamps merge**); Vector Lullaby's long phrase overflowed at size 40 → size 28 + explicit line breaks. All 6 presets + text-string shape mode + animation + SVG export (360k–917k chars, one element/particle) render with **0 console errors** (only a harmless `willReadFrequently` perf hint from `loadPixels`). Landing card copy kept; preview rewritten to a **rasterized "R"** (offscreen glyph → gradient dot grid + sine ripple). The SAMPL-flagged **TEXTR double-`ctx.scale(pd,pd)` check is still open** (RASTR avoided it by using p5's API, not raw ctx). **Next:** pick a tool from the tracker (DITHR raster / BIOM generative / DRIFT / KLON / SKAAAN image-manip) and run the method.
- **2026-05-26** — Re-ported **DITHR** end-to-end (v1 was a CPU 2D-canvas dithering approximation — the real tool is a **GPU shader pipeline**). Biggest tool so far. **Engine:** a SOURCE (a lit 3D primitive, or a dropped image) renders into a WEBGL buffer `gImg` → a **dither shader** writes `dithBuffer` → a **palette-remap shader** writes `gradBuffer` → blit to screen. Five fragment shaders, all **third-party open source kept verbatim with attribution** (Sean LeBlanc ordered-dither, Stefan Gustavson CMYK rosette halftone, humanbydefinition ASCII — MIT/PD): ASCII (glyph atlas indexed by luminance), basic halftone (per-RGB SDF dots), CMYK rosette, ordered Bayer/pattern matrix (threshold **texture** tiled), and noise-texture threshold. Then a 1-D **palette gradient** remaps luminance→colour (or 'original' keeps the dithered colour). Brightness/contrast/saturation/posterize sit between source and dither. **Stack = p5 1.9.4 WEBGL** on the shell + `p5.loadFont` for the ASCII pixel font (Silkscreen). **IP swaps:** antlii's source is OBJ **letter models** + precomputed **blue-noise PNGs** + their h264 export + a Google-Fonts picker — I ship **p5 primitives** (torus/sphere/box/cone/cylinder lit by 3 coloured directional lights — the hero look), **procedurally-generated noise textures** (seeded via `alea`), Bayer computed by the standard recurrence (+ my own pixel/grid/checker/diagonal pattern maps), my own **14 palettes**, and one OFL pixel font. **Four gotchas, each cost a cycle (all WEBGL/p5-shell-specific — document for the next shader tool):** (1) **TP4 options-binding on an undefined value throws a cryptic minified `n`** → `cnv.source` must be in the state literal before the UI builds (p5 `setup` runs async, after UI). (2) **Full-screen-quad VERT:** REFRACT's `gl_Position=vec4(aPosition,1.)` fills the *main canvas* but NOT a graphics *buffer* (there aPosition is model-space, not clip) → derive the quad from `aTexCoord` instead: `gl_Position=vec4(aTexCoord*2.-1.,0,1)` (reliably 0..1 everywhere). (3) **WEBGL-graphics→WEBGL-graphics `image()` is unreliable** (the 'none'/'original' passthroughs rendered transparent on the original setup buffers, only worked on freshly-rebuilt ones) → use a tiny **PASS_FRAG copy shader** instead of `image()` for buffer→buffer blits (main-canvas `image(buffer)` is fine). (4) **WebGL context leak**: recreating the 3 buffers on every ratio change without releasing exhausts the browser's ~16-context limit ("too many active WebGL contexts" → oldest lost → blank) → release via `WEBGL_lose_context` before recreating AND only rebuild when the size actually changes; also `applyRatio` must only run when the ratio truly differs (calling it with an unchanged ratio blanked the render). Source/dither/grad use **pixelDensity 1** with `u_resolution = width*pd` (robust either way). State shaped like the reference (`cnv/obj/motion/ascii/dither/gradient`) → reference preset objects deep-merge onto a clean default; `window.__dithr.applyPreset()` for A/B. 6 original-named presets covering all 5 dither modes + every source/ratio: **Spectral Torus** (default; Bayer + blue palette), Coarse Halftone, CMYK Press, Terminal Glyphs (ASCII), Blue Noise Grain, Diagonal Duotone. A/B-validated vs live `loadPreset('originalHalftoneCMYK')` — **identical CMYK rosette halftone character** (same Gustavson shader; only the source differs: my torus vs their OBJ "H" — intended). All 6 presets render opaque + 0 console errors; every mode visually confirmed (Bayer crosshatch / halftone dots / CMYK rosette / ASCII glyphs / noise grain). **Preset-tuning lesson (halftone):** the basic-halftone needs a paper bg + a lit object with real tonal range (lower ambient → darker shadows → visible dots); too-bright washes out, too-dark on dark bg vanishes. Landing card copy updated; preview rewritten to a **dithered palette-mapped lit sphere**. `tools/dithr/index.html` already loaded p5+TP — unchanged (no opentype needed; `p5.loadFont` handles the ASCII font). **Next:** pick a tool from the tracker (BIOM generative / DRIFT / KLON / SKAAAN image-manip) and run the method.
- **2026-05-27** — Re-ported **BIOM** end-to-end (v1 was a metaball radial-gradient "cell-growth in a Swiss poster" — structurally wrong). **Engine = orbiting forms × concentric gradient layers.** `form.amount.live` Form objects are p5-`randomSeed(form.seed)`-seeded, each with random phase offsets `xoff{x,y}`/`yoff{x,y}`, a `rotation` (−HALF_PI..HALF_PI) and a `size` (0.1..2) — **6 `random()` calls per form, in that order** (match it to reproduce a seed). Each frame every form computes ONE base position from a **Lissajous** (`sin(τ·frame·areaSpeed+xoff.x+areaPhase)+cos(…+xoff.y)`, y analogous) × `radius` (x also ×`constraint`). Then the render loops `i=0..layers` (layers = `total≤14 ? total*3 : total*7`, so default 16→112): each layer is a filled stamp of `size=map(i,…,startSize,endSize)` (large→small, so later/smaller layers paint over earlier → **crisp concentric rings**), fanned out by `posx/posy=map(i,…,1,xSpace/ySpace)` (spacing ≤0 contracts toward centre → bullseye blooms; >0 spreads), coloured by an **IQ-style sinusoidal palette**: `map(sin(gradCount·gradMult + offset.{x,y,z}·τ + τ·frame·colorSpeed + colorPhase), -1,1, lightness·255, baseColor.{r,g,b})` per channel. Background = custom colour, or the same sin formula at frame 0 (form-static) / current frame (form-animated). A polar **clip mask** (none/ellipse/rect/triangle, scaled by clip w/h + rotated) bounds the field via `beginClip/endClip` — note p5's `background()` is **clipped** by an active clip path, so outside the shape stays transparent (good: PNG export is transparent outside the mask). **Port stack = p5 1.9.4 2D only** (no paper/opentype/shaders). Render runs directly on the shell's main canvas (square render-space `GW=GH=760`, CSS-fit), no separate gForm buffer — **antlii's whole poster/branding layer (logo/text/info, MonumentExtended + PPMori fonts, watermark, license/embed-protect) is omitted**; we render only the graphics field. Drew through p5's API (ellipse/rect/triangle), not raw `drawingContext`, so SAMPL's double-pixelDensity 4× gotcha can't bite. **No SVG** (reference disabled it) and **no image/SVG drop** (BIOM's only drop is JSON presets, which the shell/export already cover). State objects (`cnv/form/palette/area/clipping`) shaped exactly like the reference → preset objects deep-merge onto a clean default; `window.__biom.applyPreset()` + `setFrame()` for A/B. Bootstrap deferred to the first `draw()` tick via `pendingPreset` (the BOIDS/DITHR init-race pattern). **TP4 win to record:** colours stored as `{r,g,b}` (0..255) objects bind **directly** with a plain `addBinding(obj,'color')` — TP4 auto-detects an RGB-object as a colour swatch (verified: 'Base Color'/'Back Color' render as pickers). So reference `{r,g,b}` presets need no hex conversion. The 4-key `palette.offset {bg,x,y,z}` is **not** a bindable point in TP4 → split into four separate 0..1 sliders (Offset R/G/B + Phase Offset), same lesson as TEXTR's `{min,max}` and BOIDS' interval. Replicated the enable/disable logic (form rotation off for ellipses; area motion/constraint/spacing off when radius=0; phase enabled only when its speed=0; clip dims off when none; back-colour vs phase-offset by bg type). Authored 6 original-named presets (numeric configs studied from the source, own names): **Crimson Cell** (default, ellipse, ellipse-clip, red rings), Magenta Bloom (rectangle, lightness-1 bright, ellipse clip squashed), Cobalt Swarm (75 forms, −1/−1 spacing → dense blue bullseye bloom), Rose Vortex (rotated unclipped, gradient-79), Solar Eye (1 huge form, animated bg, ellipse clip), Static Drift (rectangles, rect clip, animated pink→purple bg). Also ported the randomizer (`randomParams` minus poster fields) onto an OPTIONS button. A/B-validated vs live `loadPreset('biom01Preset')` frozen at frame 275 — **same family**: red ellipse-clipped cluster of concentric black/red ring cells, cell positions aligning at the matched frame (the live's softer/finer look is its smaller native gForm + a different loop-length phase, not an algorithm diff; live biom01 sets `rec.length 30`, my preset leaves the default 10 → A/B at a fixed frame differs in colour phase only). Triangle shape + triangle clip, rectangle + rect clip, both bg modes, ±spacing all confirmed; 0 console errors (only favicon 404). Landing card copy + preview rewritten to the concentric-ring cell bloom. **Next:** pick a tool from the tracker — only the 3 image-manip tools remain (DRIFT / KLON / SKAAAN, all `*-tool` live sources). Note: the SAMPL-flagged TEXTR double-`ctx.scale(pd,pd)` check is still open (BIOM avoided it by using p5's API).
- **2026-05-27** — Re-ported **DRIFT** end-to-end (1st image-manip port; v1 was a loose "capture an area, stamp it drifting" approximation). **Engine = sample image fragments → drifting Forms that accumulate trails.** You sample a rect/ellipse window of a source image and spawn a **Form**; each form owns a full-image-sized transparent `graphics` buffer + a sample-sized `buffer`/`updBuffer`. Per frame it copies the image region into `buffer`, overlays a `tint` colour, masks to an ellipse via `globalCompositeOperation='destination-in'`, then draws that onto its `graphics` at `translate + move`, rotated/scaled/opacity-tinted. **Eight animation channels** — move x/y, offset x/y, rotate, scale, opacity, tint — each with a **type** (none/const/sin/cos/noise; offset & scale have no const), a **level**, a **rate**, and a **trend** (pos/neg/random/toggle). The `mapping` table re-maps each UI range to engine magnitudes (e.g. move noise.level = `map(level,…, imgW*0.01, imgW*1)`); sin/cos use a frame-counter `geom` form, noise uses an independent `SimplexNoise` per channel per form. Two **content** modes: `preview`/static (sample fixed at the picked coords, the whole form drifts) and `live` (the sample window itself scans the image, modulo-wrapped). Two **rendering** modes that are the heart of the look: **`canvas`** clears each form's `graphics` every frame (clean moving fragment) and **`layer`** never clears it (the form's own buffer accumulates → a sweeping trail). **Crucial discovery (cost a re-read):** `gForm` — the buffer all forms composite onto — is **NOT cleared per frame** (only on clear/new-image/preset); it persists across frames, so motion *always* smears/builds up. That persistent accumulation IS the "drift." The base image is redrawn fresh each frame underneath; `gForm` (all fragment history) draws on top. `edges()` wraps a form's `translate` around the image bounds by checking its rotated corner/perimeter points. **Port specifics:** p5 1.9.4 2D; canvas sized to the **image** dimensions, pixelDensity 1, CSS-fit to viewport (`fitCanvas`). Mouse sampling done with **manual pointer→image-space mapping via `getBoundingClientRect`** (robust against the CSS scale; don't trust p5 `mouseX` through a CSS-resized canvas): pointerdown sets the sample coords, pointerup spawns, wheel resizes the window (shift = uniform). Per-channel noise = `createNoise2D(alea(random))` from the vendored simplex (independent random instances, like the reference's `new SimplexNoise()`). **IP swaps:** antlii loads a random stock photo (`getRandomImage`, their asset) + branding/watermark/license — I **procedurally generate the default image** (a warm→cool wash + HSB noise blobs + accent stripes) so the tool is self-contained; drag-drop replaces it with the user's image (the only drop the reference also takes, besides JSON presets). State (`cnv/form/ui/anim/mapping`) shaped like the reference → preset objects deep-merge onto a clean default; `window.__drift` hook. **Divergence (intentional, better UX):** the reference's preset change only sets params for *future* forms; mine **clears + respawns** the startup forms on preset apply so the preset's character shows immediately. **Preset size quirk:** preset `form.size.x/y` are on a **1–100 scale** mapped to `[size.min, imgW/H]` (relative to image), and `size.uniform` squares them — replicated in `applyPreset`. Tint/frame colours may arrive as `{r,g,b}` → `rgbaToHex`. 6 original-named presets (Drift Field default + Square Snapper + Soaring Blobs + Clockwork Fans + Continuous Scan + Red Loops) cover canvas/layer × static/live × every channel; ported the randomizer + keyboard shortcuts (= add, − remove, C clear, P pause, A randomize, I toggle image, M shape). A/B-validated vs live `loadPreset('clockworkFans')` — **same family**: thin rotating sliver samples in layer mode accumulate into concentric/radial circular fans (mine shows fuller spoke-fans with frame-on white strokes vs the live's filled discs on its photo; structure matches, my procedural image ≠ their stock photo as intended). Drift Field (canvas smear streaks), Clockwork Fans (layer radial fans), Continuous Scan (live-content vertical scan with noise wobble) all confirmed; 0 console errors (only favicon 404). Landing card copy + preview rewritten to a spinning sliver-fan with trails. **Next:** 2 image-manip tools remain — KLON, SKAAAN (both `*-tool` live sources). Still open: the TEXTR double-`ctx.scale(pd,pd)` check.
- **2026-05-28** — Re-ported **KLON** end-to-end (2nd image-manip; v1 was a "pick / paint grid-slice brush" approximation that missed the clone-stamp model). **Engine = grid-snapped clone-stamp + collage tool.** You drag on the canvas to draw a rect/ellipse/triangle (with 90° rotation); on `pointerdown` the area under the shape is sampled into `gArea` (with optional 90° rotation of the buffer + a `destination-in` shape mask → `gBuffer`); while held, the cursor "brushes" `gBuffer` onto `gResult` at the current grid-snapped `preview.coords` each frame → a trail of shape-masked image fragments. Three **select** modes — `free` (each press grabs a new area), `buffer` (first press captures, subsequent strokes paste the same fragment — `preview.select` latches), `erase` (cuts the current shape from `gResult` via `globalCompositeOperation='destination-out'`); a `draw` switch samples either the source image (`source`) or the source + the live accumulating result (`result`). The **grid** is power-of-two: cell = `2^(grid.ui.x + grid.mult)`, with `grid.mult` auto-derived per image so cell counts stay sane across resolutions; `gridX/gridY` round to cell, `grid.mod.x/y` centres the grid on the image. Render layers per frame: bg → grid (hidden while dragging) → source img → gResult → (continuous stamp into gResult while held) → gPreview overlay (the shape-clipped live preview of the area under the cursor, or `gBuffer` when dragging or in latched-buffer mode) → outline (red when erase or pre-capture buffer, black otherwise). **Stack = p5 1.9.4 2D only.** **Simplification vs reference:** the reference uses a two-coord system (display-canvas at fit-size + buffers at image-native + a `cnv.density` scale factor between them, then `form.coords = preview.coords / density`); I collapse to **single image-space coords** everywhere (canvas internal = image size, CSS-fit; `form.coords === preview.coords`) which makes the code half the size and avoids the density bookkeeping. Pointer→image-space via `getBoundingClientRect` scaling (the DRIFT lesson). **IP swaps:** antlii ships a stock cover photo + branding/watermark/license — I procedurally generate the default image (warm→cool wash + HSB noise blobs + graphic bars + horizontal slabs to give visible structure for cloning); drag-drop replaces it. **No presets** (the reference has none — it's a pure interactive tool). Ported keyboard shortcuts (R rotate, M draw mode, A reset buffer, S cycle select, E erase toggle, G grid snap, H grid show, I source, C clear, T cycle shape, Ctrl/Cmd+Z undo, arrow keys for cell counts) and shift-axis-lock (lock x or y once the user clearly moves along one axis during drag). Three shape orientations (triangle by 90° angle) and 4-state buffer rotation (independent of triangle angle) ported via `drawTriCorners`/`drawTriCenter` helpers. **Validation gotcha for next interactive tool:** synthetic `PointerEvent` dispatches all fire in one JS task → no draw-frame between them → only one stamp lands. Spread events across `requestAnimationFrame` ticks (`await raf()` between each move) to get a real trail. **A/B note:** the live tool's `mousePressed/Released` are wired via p5's `canvas.mousePressed` API and read p5's internal `mouseIsPressed` — synthetic `MouseEvent`s don't reliably flip that flag, so driving the live tool headlessly to clone-stamp is impractical. Compared static UI/preview character instead: same shape-mask preview overlay, same grid dot system, same control surface (Select Data / Draw Mode / Source / Background / Shape / Rotation / Grid Settings). Confirmed locally: ellipse trail clone (30-step drag produced a chain of circle-clipped image fragments), triangle stamps with 90° rotation produced a staircase diagonal trail, erase mode shows the red outline. All 3 shapes + 3 select modes + 2 draw modes + grid snap/show + rotation + drag-drop image + undo + clear render with **0 console errors**. Landing card copy + preview rewritten to a sweeping trail of alternating rect/triangle/ellipse stamps on a paper bg with grid dots — captures KLON's collage gesture. **Next:** **1 tool left** — SKAAAN (image-manip, `skaaan-tool` live source). Still open: the TEXTR double-`ctx.scale(pd,pd)` check.
- **2026-05-28** — Re-ported **SKAAAN** end-to-end (12th faithful port; 3rd image-manip; v1 was a slit-scan smear approximation — close in flavour but missed the channelled transform model). **Engine = scan-line image displacement.** Source image drawn into `gSource` under a stack of transforms: `maap` (mouse-drag pan, lerped via `params.easing`) → `shift x/y` (none / linear / periodic / noise) → `scale` (same channel types) → `rotation` (same), then `imgSource` drawn centred. Pressing **Start Scan** flips `scan.action`; each frame, `params.frame += scan.speed` and `gSource.get(scan.position, ...)` extracts a thin slice (scan.speed wide, area-height tall) that gets pasted into `gResult` at the same position; scan.position advances until the area ends → `scanComplete` resets `params.frame`. The trick: each channel **only advances its own frame counter while `scan.action` is true AND `params.frame` is inside that channel's `area.{min,max}` window** (`window = duration*areaMin..duration*areaMax`, snapped to scan.speed). So channels can be scheduled to animate during different sections of the scan, with their own easing curves (map2: Linear/Quadratic/Cubic/Quartic/Quintic/Sinusoidal/Exponential/Circular/Sqrt × IN/OUT/BOTH — kept verbatim, sighack/Penner, MIT) and (for noise) independent `SimplexNoise` instances per channel. The shift channel's noise range is `radians(map(noise.level, 0,100, 0, scanSize/2))` (i.e. the noise amplitude scales with the *scan area*, not absolute pixels — replicated). **Stack = p5 1.9.4 2D only.** Canvas sized to the image, CSS-fit (DRIFT/KLON pattern); pointer→image via `getBoundingClientRect`. Per-channel noise = `createNoise2D(alea(rseed()))` (vendored simplex; matches reference's `new SimplexNoise(alea(seed))`). **IP swaps:** procedural default image (vs antlii's `skaaan-cover.jpg`); drag-drop loads user's; omitted branding/watermark/license/sound. **Simplifications vs reference:** dropped the dual `layer`/`side` layout (kept just `layer`), the interactive maask-drag rect (scan area = full image), and the elaborate `shade` overlay modes (kept only the `grain` post-process — a per-pixel simplex grain applied during `modifySlice` for soft/sharp variants). The simplification keeps the engine's *core character* (channelled transforms + scan-line accumulation) without 700+ lines of UI plumbing. Authored 5 original-named presets (Liquid Slip default — noise shift x/y → vertical streaks; Vertical Bleed — linear y shift + vertical scan; Periodic Drift — sinusoidal x shift with phase; Whirl Compress — periodic scale + noise rotation; Scan Static — sharp grain + noise x). Keys: Space toggle scan, R reset, C clear result, I toggle source. **Validated:** Liquid Slip 3-second scan produced exactly the expected family — vertical rainbow streaks from noise-shifted slices accumulating (`12px scan.speed` × 1280 width = 107 frames ≈ 1.8 s at 60fps). Whirl Compress produced a distinctly different result — wavy organic compression streaks from periodic scale + noise rotation. Both confirm shift / scale / rotation channels all animate correctly through the scan-window mechanism; map2 easings + period-cycle-phase formulas faithful. **A/B caveat (same as KLON):** the live tool's mouse handlers read p5's internal `mouseIsPressed` so synthetic dispatched events don't drive it; the static UI/preset taxonomy match is the structural evidence. 0 console errors. Landing card copy + preview rewritten to a horizontal scan-line sweeping a vertically-noise-shifted source → wavy vertical-streak displacement aesthetic. **All 12 antlii-source tools are now faithfully re-ported.** Remaining: the 4 no-live-source tools (RITM/REFRACT/PLAIN/STIIL) stay as original Ksawery tools by design. Still open: the TEXTR double-`ctx.scale(pd,pd)` check (an inherited concern, not blocking).
- **2026-05-28** — Post-port cleanup pass (two items, in one go).
  - **TEXTR pd-compound fix.** Verified the SAMPL-flagged gotcha applies to TEXTR: hooking `ctx.scale` showed the transform was `scale(4,4)` inside the draw (p5 1.9.4 leaves a `scale(pd,pd)` between frames; `ctx.scale(pd,pd)` compounds to pd²). Replaced `ctx.scale(p.pixelDensity(), p.pixelDensity())` with `ctx.setTransform(pd,0,0,pd,0,0)` (the absolute-set fix). Before/after screenshots showed the expected positional shift — letters now distribute correctly across the canvas instead of crowding the right edge. (RASTR / BIOM / DRIFT / KLON / SKAAAN don't have the bug — they either go through p5's API or use pixelDensity-1 graphics buffers; only SAMPL and TEXTR used the raw `getContext('2d')` + `ctx.scale(pd,pd)` pattern.)
  - **Debug-gated dev hooks.** Added `isDebug` + `exposeDebug(name, obj)` to `js/antlii/shell.js` — `exposeDebug` only attaches `window.__<name>` when the URL has `?debug`. Migrated all 12 tools (flake/splitx/boids/bluur/textr/sampl/rastr/dithr/biom/drift/klon/skaaan) from direct `window.__<name> = {...}` to `exposeDebug('<name>', {...})`. Cache caveat: ES modules are stickily cached in the browser; the all-tools verification ran with `CDPSession.Network.setCacheDisabled(true)` to force fresh fetches. Confirmed across all 12 tools: default URL → `Object.keys(window).filter(k=>k.startsWith('__'))` is `[]`; `?debug` URL → the named hook is present with its functions intact. **0 page errors across all tools.** A/B-driving the live antlii.work counterparts still works: just navigate to e.g. `/tools/flake-tool/?debug` to opt in.
